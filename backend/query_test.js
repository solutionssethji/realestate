const admin = require('firebase-admin');
const serviceAccount = require('./serviceAccountKey.json'); // wait, I don't have this.
// I can just run it using the functions directory which already has firebase-admin initialized if run via firebase functions shell?
