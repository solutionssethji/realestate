targets:
  $default:
    builders:
      # drift_dev:
      #   options:
      #     store_date_time_values_as_text: true
      #     # To use ISO 8601 strings
      #     # store_date_time_values_as_text: true
      freezed:
        options:
          generic_argument_factories: true
          explicit_to_json: true
      # riverpod_generator options removed
      # graphql_codegen:
      #   options:
      #     scalars:
      #       Upload:
      #         type: MultipartFile
      #         fromJsonFunctionName: fileFromJson
      #         toJsonFunctionName: fileToJson
      #         import: package:nyburs/scalars/multi_part.scalar.dart
      #       DateTime:
      #         type: String
      #       DateTimeISO:
      #         type: String
      #       JSONObject:
      #         type: String
      #     clients:
      #       - graphql
      #       - graphql_flutter
